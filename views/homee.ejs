<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog App</title>
  <!-- Include Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <!-- Custom CSS -->
  <style>
    body {
      background-color: #f9f9f9;
      color: #343030;
      font-family: Arial, sans-serif;
    }

    .navbar {
      background-color: #343030;
      padding: 10px 20px;
    }

    .navbar a {
      color: #fff;
      font-size: 18px;
      margin-right: 20px;
    }

    .navbar a:last-child {
      margin-right: 0;
    }

    .navbar a:hover {
      color: #ccc;
    }

    .dropdown {
      position: relative;
    }

    .dropdown .dropbtn {
      font-size: 16px;
      border: none;
      outline: none;
      color: #fff;
      background-color: #333;
      padding: 14px 16px;
    }

    .dropdown-content {
      z-index: 1;
      display: none;
      position: absolute;
      background-color: #333;
      min-width: 160px;
      box-shadow: 0px 8px 16px 0px rgba(72, 71, 71, 0.2);
      right: 0;
    }

    .dropdown-content a {
      color: #fff;
      padding: 12px 16px;
      text-decoration: none;
      display: block;
      text-align: left;
      font-size: 14px;
    }

    .dropdown-content a:hover {
      background-color: #444;
    }

    .dropdown:hover .dropdown-content {
      display: block;
    }

    .card {
      border: 1px solid #ddd;
      border-radius: 8px;
      background-color: #fff;
      transition: box-shadow 0.3s ease;
    }

    .card:hover {
      box-shadow: 0 4px 8px rgba(73, 71, 71, 0.1);
    }

    .card-title {
      color: #222;
      font-size: 24px;
      font-weight: bold;
    }

    .card-text {
      color: #333;
      font-size: 16px;
    }

    .card-date {
      color: #666;
      font-size: 14px;
    }

    .icons a {
      color: #555;
      font-size: 20px;
      transition: color 0.3s ease;
    }

    .icons a:hover {
      color: #343030;
    }

    .footer {
      background-color: #333;
      color: #fff;
      padding: 10px 0;
    }

    .btn-primary {
      background-color: #333;
      border-color: #333;
    }

    .btn-primary:hover {
      background-color: #444;
      border-color: #444;
    }
  </style>
</head>
<body>
  <header>
    <nav class="navbar navbar-expand-lg navbar-dark">
      <div class="container">
        <a class="navbar-brand" href="#">Blog App</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="">About</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/user/login">Create Post</a>
            </li>
          </ul>
        </div>
        <div class="dropdown">
          <button class="dropbtn">Login</button>
          <div class="dropdown-content">
            <a href="/user/login">User</a>
            <a href="/admin/login">Admin</a>
            <a href="/manager/login">Topic Manager</a>
          </div>
        </div>
      </div>
    </nav>
  </header>


  <main class="container mt-5">
    <div class="row">

      <% if (typeof res !== 'undefined' && res.length > 0){ %>
        <% res.forEach((ress) => { %>
          <!-- Example Post Card -->
          <div class="col-md-6 mb-4">
            <div class="card">
              <div class="card-body d-flex justify-content-between flex-column">
                <h3 class="card-title"><%= ress.title %></h3>
                <span class="card-date">Posted by <%= ress.userName %></span>
                <span class="card-date">Posted on <%= ress.date %></span>

                <p class="card-text"><%= ress.description %></p>

                <div class="d-flex mt-3 mr-4 icons">
                  <a href="/user/login" class="ms-2 me-2"><box-icon name='heart'></box-icon></a>
                  <a href="/user/login" class="me-2"><box-icon name='comment'></box-icon></a>
                </div>

                <div class="ms-auto">
                  <a href="/user/login" class="btn btn-primary">Read More</a>
                </div>
              </div>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <!-- Show some message if there are no posts -->
      <% } %>

      <!-- You can add more post cards dynamically through the backend -->
    </div>
  </main>

  <footer class="footer text-center py-3">
    <!-- Footer content goes here -->
    <p>&copy; 2023 Blog App. All rights reserved.</p>
  </footer>

  <!-- Include Bootstrap JS and Popper.js -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <!-- Include Bootstrap Icons library -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.js"></script>
  <script src="https://unpkg.com/boxicons@2.1.4/dist/boxicons.js"></script>

  <!-- Add your page content below this line -->
</body>
</html>
